const themeColor = '#6ab7ff';

const bgColor = '#fff';
const selectBgColor = '#f3f9ff';

const textColor = '#4a5366';

const borderColor = '#e4e5ea';
const selectBorderColor = '#6ab7ff';

export {
  themeColor,
  borderColor,
  textColor,
  selectBorderColor,
  bgColor,
  selectBgColor,
};
